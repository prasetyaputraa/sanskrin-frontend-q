class dictionaryWord {
  constructor (sanskrit, pos, translations, length) {
    this.sanskrit = sanskrit
    this.pos = pos
    this.translations = translations
    this.length = length
  }
}

export default dictionaryWord
